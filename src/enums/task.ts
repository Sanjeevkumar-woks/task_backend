export enum TaskPriorityEnum {
  Low = "Low",
  Medium = "Medium",
  High = "High",
}

export enum TaskStatusEnum {
  Todo = "Todo",
  InProgress = "InProgress",
  Done = "Done",
  Freezed = "Freezed",
}
